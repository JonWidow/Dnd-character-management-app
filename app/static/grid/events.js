// events.js
import { stage, gridLayer } from './grid.js';

// simple panning is already enabled via `draggable: true`
// you could add zoom/pinch later here
export function setupEvents() {
    // placeholder for pinch-to-zoom or other interactions
    console.log("Event handlers initialized");
}
